[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm[toml]>=3.4"]

[tool.black]
line-length = 88
skip-string-normalization = true
target-version = ['py36']

[flake8]
max-line-length = 88
